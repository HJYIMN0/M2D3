using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Esercizio3_Script : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int a = 0;          //Questa è una variabile con un numero intero
        float b = 0.5f;     //Questa è una variabile con un numero con la virgola
        Debug.Log("Questa è una variabile intera" + a);
        Debug.Log("Questa è una variabile con virgola" + b);
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
